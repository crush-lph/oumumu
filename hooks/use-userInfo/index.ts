/** 用户信息 */
const useUserInfo = () => {
  const userInfo = {};

  const updateUserInfo = async () => {
    // TODO: 请求接口获取用户信息，返回响应体
    return;
  };

  return { userInfo, updateUserInfo };
};

export default useUserInfo;
